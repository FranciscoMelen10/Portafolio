---
interface Props {
  name: string;
  url: string;
  img: string;
}
const { name, url, img } = Astro.props;
---

<a href={url} class="bg-[#0B0B0B] rounded-full p-3">
  <img class="size-[30px]"src={img} alt={name} loading="lazy"/>
</a>
